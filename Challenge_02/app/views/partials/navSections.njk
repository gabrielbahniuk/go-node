      <nav>
            <div class="sessions">
            {% for section in sections %}
                <a class="{% if section.id == currentSection.id%}active{% endif %}" href="/app/projects/{{ activeProject }}/sections/{{ section.id }}">
                  {{ section.title }}
                </a>
            {% endfor %}
            </div>
              <button type="submit" data-modal-open="#create-section" class="btn">Add Section <img src="../../../../img/plus.svg"></button>
      </nav>
          <div class="modal" id="create-section">
            <form class="modal-content" method="post"
                  action="/app/projects/{{ project.id }}/sections/create">
              <input type="text" name="title" placeholder="Titulo da seção">
              <div class="content-container">
                <textarea name="content" class="mde" placeholder="Conteúdo da seção"></textarea>
              </div>
              <button type="submit">SALVAR SECTION</button>
              <a href="#" data-modal-close="#create-section">CANCELAR</a>
            </form>
        </div>
