      <nav>
            <div class="sessions">
            {% for section in sections %}
                <a href="/app/project/{{ activeProject }}/sections/{{ section.id }}">{{ section.title }}</a>
            {% endfor %}
            </div>

            <form action="" method="post">
              <input type="text" name="name" placeholder="Nova seção">
              <button type="submit" class="btn"><img src="../../img/plus.svg"></button>
            </form>
      </nav>
